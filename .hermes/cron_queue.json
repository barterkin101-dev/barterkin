[
  {
    "id": "91",
    "title": "Add first-trade completion quest with credit reward — retention/gamification",
    "description": "Add a new quest `quest_first_trade` worth 15 credits, awarded automatically when a member marks their first trade as mutually completed. Update `lib/quests.ts` with the new quest def, wire it into the trade completion flow in `lib/actions/trade-completions.ts`, and add unit tests covering the award logic (idempotent, only on first trade). Track completion in PostHog. Acceptance: quest appears in dashboard QuestCard, credits awarded on first mutual trade completion, no duplicate awards, tests pass.",
    "priority": "high",
    "status": "completed",
    "notes": "Added the quest_first_trade reward to mutual trade completion, surfaced it in quests, and covered eligibility + award behavior in unit tests. Commit: 9087cd5."
  },
  {
    "id": "92",
    "title": "Add directory upgrade banner when free members hit contact limit — monetization",
    "description": "Show a sticky banner on /directory and /listings for free members who have exhausted their monthly contact limit. Banner surfaces Premium pricing, savings math, and a direct CTA to /dashboard/billing. Use live contact-limit status from `lib/data/contact-limit.ts`. Track impression and click in PostHog. Acceptance: banner only shows for free members at limit, uses live plan values, links to billing, unit tests cover visibility logic and CTA.",
    "priority": "high",
    "status": "completed",
    "notes": "Added a sticky Premium upsell banner on /directory and /listings for free members at the contact cap, with live pricing math and PostHog click/impression tracking. Commit: 926d193."
  },
  {
    "id": "93",
    "title": "Add listing feature/boost credit sink for premium members — monetization/engagement",
    "description": "Let premium members spend 5 credits to 'feature' a listing for 7 days. Featured listings get a visual badge and a small ranking boost in discover/directory. Add `featured_until` timestamp to listings table, a 'Feature this listing' action in the listing edit flow, and deduct credits via the existing credit ledger. Gate to premium members only. Acceptance: feature action visible only to premium, credits deducted, badge renders on featured listings, ranking boost applied, unit tests cover gating and credit deduction.",
    "priority": "medium",
    "status": "pending",
    "notes": ""
  },
  {
    "id": "94",
    "title": "[codex-review FIX_FIRST] [task-67 codex-fallback] add unread message reminder banner (03f870fe)",
    "description": "codex flagged issues in 03f870fe. Address concerns, recommit with [task-94 fix-03f870fe] prefix.\n\n47\t\n    48\t  const needsDailyLoginSync = profile ? !isUtcDateToday(profile.last_login_at) : false\n    49\t  const streakResult = needsDailyLoginSync ? await updateLoginStreak() : null\n    50\t\n    51\t  const [listings, messageCount, , referralRows, creditBalance, foundingCountResult, discoverResult, latestResult, questCompletions, conversations] = profile ? await Promise.all([\n    52\t    getMyListings(profile.id),\n    53\t    supabase\n    54\t      .from('conversation_participants')\n    55\t      .select('conversation_id', { count: 'exact', head: true })\n    56\t      .eq('profile_id', profile.id)\n    57\t      .then(({ count, error }) => {\n    58\t        if (error) {\n    59\t          const log = createLogger('dashboard')\nexec\n/bin/bash -lc \"nl -ba lib/data/dashboard-reminders.ts | sed -n '1,140p'\" in /root/barterkin succeeded in 0ms:\n     1\timport type { ListingRow } from '@/lib/data/listings.types'\n     2\timport type { ConversationRow } from '@/lib/data/messaging'\n     3\t\n     4\tconst TWENTY_FOUR_HOURS_MS = 24 * 60 * 60 * 1000\n     5\tconst FOURTEEN_DAYS_MS = 14 * 24 * 60 * 60 * 1000\n     6\t\n     7\texport interface UnreadMessageReminder {\n     8\t  unreadConversationCount: number\n     9\t  unreadMessageCount: number\n    10\t  href: string\n    11\t  counterpartName: string\n    12\t  lastMessageAt: string\n    13\t}\n    14\t\n    15\texport interface StaleListingReminder {\n    16\t  staleListingCount: number\n    17\t  href: string\n    18\t  listingTitle: string\n    19\t  listingId: string\n    20\t  staleSince: string\ncodex\nThere’s a potentially stronger correctness angle than the banner itself: the conversation data helper depends on reading the other participant row out of `conversation_participants`. I’m verifying whether later migrations relaxed that RLS policy, because if not the reminder can never know the counterpart name and may already be relying on inaccessible rows.\nexec\n/bin/bash -lc 'rg -n \"conversation_participants.*policy|create policy .*conversation_participants|alter polic",
    "priority": "critical",
    "status": "completed",
    "created_at": "2026-05-14T09-28-01Z",
    "source": "codex-review",
    "notes": "Unread reminder now falls back to the last unread sender name instead of relying on hidden counterpart participant rows; added regression coverage. Commit: f86571b."
  },
  {
    "id": "95",
    "title": "[codex-review FIX_FIRST] [task-66 codex-fallback] add referral share tracking (f6a216d4)",
    "description": "codex flagged issues in f6a216d4. Address concerns, recommit with [task-95 fix-f6a216d4] prefix.\n\nre stayed stable in this commit. I’m checking the dashboard callsite and test/runtime constraints now, especially browser API assumptions and whether the tests would miss a real production failure.\nexec\n/bin/bash -lc \"sed -n '360,430p' app/'(app)'/dashboard/page.tsx\" in /root/barterkin succeeded in 0ms:\n\n      {staleListingReminder && (\n        <StaleListingReminder reminder={staleListingReminder} />\n      )}\n\n      {digestOptOutReminder && (\n        <DigestOptOutReminder href={digestOptOutReminder.href} />\n      )}\n\n      {profile && profileViewsSnapshot && (\n        <ProfileViewsSnapshotCard\n          isPublished={profile.is_published}\n          snapshot={profileViewsSnapshot}\n        />\n      )}\n\n      {/* Profile completion nudge */}\n      {profile && (\n        <Card className={profile.is_published ? 'border-border' : 'border-amber-200 bg-amber-50'}>\n          <CardContent className=\"p-6\">\nexec\n/bin/bash -lc \"git show f6a216d4eb9940756fbaaf5a8fd6c62ab6c851e0:tests/unit/referral-invite-card.test.tsx | sed -n '1,220p'\" in /root/barterkin succeeded in 0ms:\nimport { render, screen, waitFor } from '@testing-library/react'\nimport userEvent from '@testing-library/user-event'\nimport { beforeEach, describe, expect, it, vi } from 'vitest'\nimport { ReferralInviteCard } from '@/components/dashboard/ReferralInviteCard'\n\nconst mockCapture = vi.fn()\nconst mockWriteText = vi.fn()\nconst mockShare = vi.fn()\n\nvi.mock('@/lib/analytics-client', () => ({\n  captureClientEvent: (...args: unknown[]) => mockCapture(...args),\n}))\n\ndescribe('ReferralInviteCard', () => {\n  beforeEach(() => {\n    mockCapture.mockClear()\n    mockWriteText.mockReset()\n    mockShare.mockReset()\n\n    Object.defineProperty(window.navigator, 'clipboard', {\nexec\n/bin/bash -lc 'cat package.json' in /root/barterkin succeeded in 0ms:\n{\n  \"name\": \"barterkin\",\n  \"version\": \"0.1.0.0\",\n  \"private\": true,\n  \"engines\": {\n    \"node\": \">=20.0.0\",\n    \"pnpm\": \">=10.0.0\"\n  },\n  \"scripts\": {\n    \"dev\": \"next dev\",\n    \"build\": \"",
    "priority": "critical",
    "status": "completed",
    "created_at": "2026-05-14T09-28-01Z",
    "source": "codex-review",
    "notes": "Removed unread reminder dependence on hidden conversation_participants rows and covered the RLS-safe fallback path in unit tests. Commit: 8530e11."
  },
  {
    "id": "96",
    "title": "[codex-review FIX_FIRST] [task-65 codex-fallback] add dormant re-engagement cron (df1e53cc)",
    "description": "codex flagged issues in df1e53cc. Address concerns, recommit with [task-96 fix-df1e53cc] prefix.\n\nl } from '@/lib/utils/disposable-email'\nimport { validateAndSanitize } from '@/lib/utils/validation'\nimport { getClientIp, limitAuthRequest } from '@/lib/rate-limit-public'\nimport { createLogger } from '@/lib/utils/logger'\nimport { captureEvent } from '@/lib/analytics'\nimport { Resend } from 'resend'\n\nexport type { JoinWaitlistResult }\n\nexport async function joinWaitlist(\n  _prev: JoinWaitlistResult | null,\n  formData: FormData,\n): Promise<JoinWaitlistResult> {\n  const log = createLogger('waitlist')\n  const parsed = validateAndSanitize(WaitlistSchema, {\nexec\n/bin/bash -lc \"sed -n '1,220p' lib/supabase/admin.ts\" in /root/barterkin succeeded in 0ms:\nimport 'server-only'\nimport { createClient as createSupabaseClient } from '@supabase/supabase-js'\nimport type { Database } from '@/lib/database.types'\n\n// Service-role client bypasses RLS. Use only from Supabase Edge Functions or\n// explicitly-gated server routes. Plan 05 does NOT use this; Phase 5 contact\n// relay lives in a Supabase Edge Function where the service-role key is\n// supplied by Supabase's managed secrets store, NOT this bundle.\n//\n// Public repo rule: never remove `import 'server-only'` from line 1.\n//\n// Lazy initialization: the client is only created on first access so that\n// build-time static analysis (e.g. Next.js `next build`) doesn't fail when\n// SUPABASE_SERVICE_ROLE_KEY is absent in CI.\nlet _client: ReturnType<typeof createSupabaseClient<Database>> | null = null\n\nexport function getSupabaseAdmin() {\n  if (_client) return _client\n  const url = process.env.NEXT_PUBLIC_SUPABASE_URL\n  const key = process.env.SUPABASE_SERVICE_ROLE_KEY\nexec\n/bin/bash -lc \"sed -n '1600,1675p' lib/database.types.ts\" in /root/barterkin succeeded in 0ms:\n        Args: { p_profile_id: string }\n        Returns: {\n          credits_earned: number\n          is_new_day: boolean\n          streak: number\n        }[]\n      }\n      update_webhook_secret: { Args: never; Returns: undefined }\n      utc_day: { Args: { ts: string }; Return",
    "priority": "critical",
    "status": "completed",
    "created_at": "2026-05-14T09-28-01Z",
    "source": "codex-review",
    "notes": "Moved re-engage admin-client initialization inside the guarded route path so missing service-role env returns JSON 500 instead of crashing; added regression coverage. Commit: d5f0ca7."
  },
  {
    "id": "97",
    "title": "[codex-review FIX_FIRST] [task-64 codex-fallback] ship quest streaks and secure rewar (498a8672)",
    "description": "codex flagged issues in 498a8672. Address concerns, recommit with [task-97 fix-498a8672] prefix.\n\nsql:150:grant execute on function public.find_conversation_between(uuid, uuid) to service_role;\nlib/actions/profile.types.ts:32:export type SkillOfferedRow = Database['public']['Tables']['skills_offered']['Row']\nlib/actions/profile.types.ts:36:  skills_offered: SkillOfferedRow[]\nsupabase/migrations/20260514090000_referral_conversion_rewards.sql:4:create or replace function public.award_referral_credits(p_invitee_id uuid)\nsupabase/migrations/20260514090000_referral_conversion_rewards.sql:38:  insert into public.credit_ledger (profile_id, amount, reason, referral_id)\nsupabase/migrations/20260514090000_referral_conversion_rewards.sql:45:comment on function public.award_referral_credits(uuid) is\nsupabase/migrations/20260420224326_directory_search.sql:3:-- Depends on: 003_profile_tables.sql (profiles, skills_offered, skills_wanted, public.current_user_is_verified())\ncodex\nI’ve isolated a likely SQL security issue and I’m confirming whether it’s already mitigated elsewhere or if this migration leaves the RPCs callable beyond authenticated users. I’m also checking whether the daily streak logic matches “UTC day” as claimed, or just elapsed hours.\nexec\n/bin/bash -lc \"sed -n '1,180p' supabase/migrations/20260513010000_referral_credits.sql\" in /root/barterkin succeeded in 0ms:\n-- Referral credit tracking for the invite-a-friend viral loop.\n-- Awards credits to both inviter and invitee when onboarding completes.\n\n-- Add credits column to profiles for quick balance lookups.\nalter table public.profiles\n  add column if not exists credits integer not null default 0;\n\n-- Track who referred whom and whether credit has been awarded.\ncreate table if not exists public.referrals (\n  id uuid primary key default gen_random_uuid(),\n  inviter_id uuid not null references public.profiles(id) on delete cascade,\n  invitee_id uuid not null references public.profiles(id) on delete cascade,\n  invitee_referral_code text not null,\n  credited_at timestamptz,\n  created_at timestamptz not null default ",
    "priority": "critical",
    "status": "completed",
    "created_at": "2026-05-14T09-28-01Z",
    "source": "codex-review",
    "notes": "Added a follow-up migration that moves login streak progression to UTC calendar days and explicitly restricts referral/streak RPC execution to authenticated + service_role; added regression coverage. Commit: 2f5158d."
  },
  {
    "id": "98",
    "title": "[codex-review FIX_FIRST] [task-64 codex-fallback] wire quest awards into actions (cd839e98)",
    "description": "codex flagged issues in cd839e98. Address concerns, recommit with [task-98 fix-cd839e98] prefix.\n\nsucceeded in 0ms:\n   170\t\n   171\t  if (values.images.length > 0) {\n   172\t    const imageRows = values.images.map((url, i) => ({\n   173\t      listing_id: listing.id,\n   174\t      url,\n   175\t      sort_order: i,\n   176\t    }))\n   177\t    const { error: insertImagesErr } = await supabase\n   178\t      .from('listing_images')\n   179\t      .insert(imageRows)\n   180\t    if (insertImagesErr) {\n   181\t      const log = createLogger('listings')\n   182\t      log.warn('image insert failed', { error: insertImagesErr, context: { code: insertImagesErr.code } })\n   183\t    }\n   184\t  }\n   185\t\n   186\t  revalidatePath('/dashboard/listings')\n   187\t  revalidatePath('/listings')\n   188\t  revalidatePath(`/listings/${listing.id}`)\n   189\t\nexec\n/bin/bash -lc \"nl -ba lib/actions/profile.ts | sed -n '228,350p'\" in /root/barterkin succeeded in 0ms:\n   228\texport async function setPublished(\n   229\t  _prev: SetPublishedResult | null,\n   230\t  formData: FormData,\n   231\t): Promise<SetPublishedResult> {\n   232\t  const supabase = await createClient()\n   233\t  const {\n   234\t    data: { user },\n   235\t    error: authError,\n   236\t  } = await supabase.auth.getUser()\n   237\t  if (authError || !user) return { ok: false, error: 'Not authenticated.' }\n   238\t\n   239\t  const parsed = SetPublishedSchema.safeParse({\n   240\t    profileId: formData.get('profileId'),\n   241\t    publish: formData.get('publish'),\n   242\t  })\n   243\t  if (!parsed.success) return { ok: false, error: 'Invalid request.' }\n   244\t  const { profileId, publish } = parsed.data\n   245\t  const shouldPublish = publish === 'true'\n   246\t\n   247\t  // If unpublishing, RLS allows it directly.\ncodex\nI found one real semantic/data-integrity problem in the new hook: the listing quest is triggered on “any new listing” and the quest service only checks “has at least one listing,” so it can award a “first listing” bonus to members creating their second or tenth listing after rollout. I’m pulling exact lines for that and checking whether there’",
    "priority": "critical",
    "status": "pending",
    "created_at": "2026-05-14T09-28-01Z",
    "source": "codex-review"
  }
]
