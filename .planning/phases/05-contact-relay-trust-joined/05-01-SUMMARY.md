---
phase: "05-contact-relay-trust-joined"
plan: "01"
subsystem: "contact-relay-trust"
tags: ["schemas", "types", "test-stubs", "shadcn", "react-email", "wave-0"]
dependency_graph:
  requires: []
  provides:
    - "lib/schemas/contact.ts (MessageSchema, ReportReasonEnum, ReportSchema, BlockSchema)"
    - "lib/actions/contact.types.ts (SendContactResult, BlockMemberResult, ReportMemberResult, MarkContactsSeenResult)"
    - "components/ui/sheet.tsx (shadcn Sheet primitive)"
    - "components/ui/dropdown-menu.tsx (shadcn DropdownMenu primitive)"
    - "components/ui/select.tsx (shadcn Select primitive)"
    - "tests/e2e/fixtures/contact-helpers.ts (createVerifiedPair, cleanupPair, setAcceptingContact, setBanned, insertBlock, countContactRequests)"
  affects:
    - "Plans 05-02 through 05-06 (all consume schemas, types, and fixture helpers)"
tech_stack:
  added:
    - "@react-email/components@1.0.12 (runtime dep — Wave 2 email template)"
    - "react-email@6.0.0 (devDep — email preview CLI)"
    - "components/ui/sheet.tsx (shadcn new-york, Radix-backed)"
    - "components/ui/dropdown-menu.tsx (shadcn new-york, Radix-backed)"
    - "components/ui/select.tsx (shadcn new-york, Radix-backed)"
  patterns:
    - "Zod schema as single source of truth (client + server + Edge Function re-validation)"
    - "Result envelope types with code discriminant for inline UI error copy"
    - "@ts-expect-error on Wave 1 row aliases (removed after supabase db push + type regen)"
    - "describe.skip / test.skip stubs with FILLED IN: Plan NN comments for Wave 0 scaffolding"
key_files:
  created:
    - "lib/schemas/contact.ts"
    - "lib/actions/contact.types.ts"
    - "tests/unit/contact-schema.test.ts"
    - "tests/unit/contact-response-shape.test.ts"
    - "tests/unit/resend-webhook.test.ts"
    - "tests/unit/contact-eligibility.test.ts"
    - "tests/unit/reports-rls.test.ts"
    - "tests/e2e/contact-relay.spec.ts"
    - "tests/e2e/contact-rate-limit.spec.ts"
    - "tests/e2e/block-flow.spec.ts"
    - "tests/e2e/report-flow.spec.ts"
    - "tests/e2e/unseen-badge.spec.ts"
    - "tests/e2e/ban-enforcement.spec.ts"
    - "tests/e2e/fixtures/contact-helpers.ts"
  modified:
    - "package.json (added @react-email/components, react-email, shadcn primitives via pnpm install)"
    - "pnpm-lock.yaml (updated lockfile)"
    - "eslint.config.mjs (added .claude/worktrees/** to global ignores)"
    - "components/ui/sheet.tsx (generated by shadcn)"
    - "components/ui/dropdown-menu.tsx (generated by shadcn)"
    - "components/ui/select.tsx (generated by shadcn)"
decisions:
  - "Used @react-email/components@1.0.12 (latest compatible with Resend 6.x despite deprecation warning; no newer non-deprecated alternative exists for Resend 6.x integration at this time)"
  - "Added .claude/worktrees/** to eslint.config.mjs globalIgnores — parallel agent worktrees were causing pnpm lint failures on their own files (pre-existing, out-of-scope per SCOPE BOUNDARY rule, but blocking lint verification)"
  - "@ts-expect-error on ContactRequestRow/BlockRow/ReportRow aliases — tables added in Wave 1 migration (Plan 05-02); comments instruct removal after type regen"
metrics:
  duration: "~7 minutes"
  completed: "2026-04-21"
  tasks_completed: 3
  files_created: 14
  files_modified: 6
---

# Phase 05 Plan 01: Wave 0 Foundation — Schemas, Types, and Test Stubs

Wave 0 foundation for Phase 5 — shadcn UI primitives + react-email installed, Zod schemas and TypeScript result types authored, and all VALIDATION.md test stubs created with discoverable skip markers.

## Tasks Completed

### Task 1: Install shadcn primitives + react-email dependencies (commit: e2ffde5)

**shadcn primitives added (new-york style, Radix-backed, lucide icons):**
- `components/ui/sheet.tsx` — exports Sheet, SheetTrigger, SheetClose, SheetContent, SheetHeader, SheetFooter, SheetTitle, SheetDescription
- `components/ui/dropdown-menu.tsx` — exports DropdownMenu, DropdownMenuPortal, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuGroup, DropdownMenuLabel, DropdownMenuItem, DropdownMenuCheckboxItem, DropdownMenuRadioGroup, DropdownMenuRadioItem, DropdownMenuSeparator, DropdownMenuShortcut, DropdownMenuSub, DropdownMenuSubTrigger, DropdownMenuSubContent
- `components/ui/select.tsx` — exports Select, SelectContent, SelectGroup, SelectItem, SelectLabel, SelectScrollDownButton, SelectScrollUpButton, SelectSeparator, SelectTrigger, SelectValue

**Dependencies installed:**
- `@react-email/components@1.0.12` — runtime dep (used in Edge Function email template, Wave 2)
- `react-email@6.0.0` — devDep (email preview CLI, local development only)

### Task 2: Author Zod schemas and TypeScript result types (commit: 67171b8)

**lib/schemas/contact.ts:**
- `MessageSchema` — CONT-02/03: recipientProfileId (UUID) + message (trim, 20–500 chars) with exact copywriting error messages from UI-SPEC
- `ReportReasonEnum` — TRUST-01: `z.enum(['harassment', 'spam', 'off-topic', 'impersonation', 'other'])`
- `ReportSchema` — TRUST-01/05: targetProfileId + reason + optional bounded note (max 500 chars)
- `BlockSchema` — TRUST-02: blockedOwnerId (UUID) + blockedDisplayName + blockedUsername

**lib/actions/contact.types.ts:**
- `SendContactResult` — 12-code discriminant matching Edge Function rejection codes
- `BlockMemberResult` — 4-code discriminant
- `ReportMemberResult` — 4-code discriminant + fieldErrors
- `MarkContactsSeenResult` — with optional count
- `ContactRequestRow`, `BlockRow`, `ReportRow` — Wave 1 row aliases with `@ts-expect-error` (tables added in Plan 05-02)

**tests/unit/contact-schema.test.ts:** 25 passing tests covering all message boundary conditions, enum values, and block/report schema validation.

### Task 3: Create Wave 0 test stubs (commit: 4c4a685)

**4 unit test stubs (describe.skip with FILLED IN comments):**
- `tests/unit/contact-response-shape.test.ts` — CONT-06 privacy invariant (FILLED IN: Plan 03)
- `tests/unit/resend-webhook.test.ts` — CONT-09 webhook signature verification (FILLED IN: Plan 03)
- `tests/unit/contact-eligibility.test.ts` — TRUST-07 eligibility RPC (FILLED IN: Plan 02)
- `tests/unit/reports-rls.test.ts` — TRUST-05 reports table RLS (FILLED IN: Plan 02)

**6 E2E test stubs (test.skip with FILLED IN comments):**
- `tests/e2e/contact-relay.spec.ts` — CONT-01/04, 5 tests (FILLED IN: Plan 05 + 03)
- `tests/e2e/contact-rate-limit.spec.ts` — CONT-07/08, 3 tests (FILLED IN: Plan 03 + 05)
- `tests/e2e/block-flow.spec.ts` — TRUST-02, 4 tests (FILLED IN: Plan 04 + 05)
- `tests/e2e/report-flow.spec.ts` — TRUST-01/06, 5 tests (FILLED IN: Plan 04 + 05)
- `tests/e2e/unseen-badge.spec.ts` — CONT-10, 4 tests (FILLED IN: Plan 06)
- `tests/e2e/ban-enforcement.spec.ts` — TRUST-03, 4 tests (FILLED IN: Plan 03 + 05)

**Playwright discovers 25 skipped E2E tests across 6 files.**

**tests/e2e/fixtures/contact-helpers.ts** — Full implementation (not stubbed):
- `createVerifiedPair(prefix)` — creates 2 verified auth users + published profiles
- `cleanupPair(senderId, recipientId)` — deletes auth users (cascades to all FK tables)
- `setAcceptingContact(ownerId, accepting)` — flips `profiles.accepting_contact`
- `setBanned(ownerId, banned)` — flips `profiles.banned`
- `insertBlock(blockerOwnerId, blockedOwnerId)` — inserts into `blocks` ON CONFLICT DO NOTHING
- `countContactRequests({ recipientId, unseenOnly? })` — count for badge + relay assertions

## Final Verification State

| Check | Result |
|-------|--------|
| `pnpm test` | 128 passing, 33 skipped, 0 failures |
| `pnpm typecheck` | 0 errors |
| `pnpm lint` | 0 errors (29 warnings from pre-existing stubs/unused params) |
| Playwright discovers 6 new E2E spec files | 25 tests listed |
| All 3 shadcn primitives present | confirmed |
| `@react-email/components` in dependencies | confirmed |
| `react-email` in devDependencies | confirmed |
| 10 stub files contain `FILLED IN: Plan 0` | confirmed (grep -l returns 10) |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added `.claude/worktrees/**` to eslint.config.mjs globalIgnores**
- **Found during:** Task 1 (pnpm lint verification)
- **Issue:** `pnpm lint` was failing with errors from other parallel agent worktrees (`.claude/worktrees/agent-*/`) — these directories contain other agents' code not related to this task and were picked up by ESLint traversal
- **Fix:** Added `.claude/worktrees/**` to `globalIgnores` in `eslint.config.mjs`
- **Files modified:** `eslint.config.mjs`
- **Commit:** e2ffde5

**2. [Note] `@react-email/components@1.0.12` deprecation warning**
- **Context:** The package shows a deprecation warning ("Package no longer supported") but is still the correct version for Resend 6.x integration. The plan explicitly noted this scenario ("let pnpm install the latest compatible with Resend 6.x"). No action needed — this is the expected outcome.
- **Impact:** Zero runtime impact. Wave 2 (email template) may need to revisit if a successor package is identified at that point.

## Known Stubs

All stubs are intentional scaffolding for future waves. Each has a `FILLED IN: Plan NN` comment pointing to the implementing plan:
- Unit stubs (4 files): Plans 02 and 03
- E2E stubs (6 files): Plans 03, 04, 05, and 06
- `ContactRequestRow`, `BlockRow`, `ReportRow` aliases: `@ts-expect-error` removed in Plan 02 after Wave 1 migration + type regen

## Threat Flags

None — this plan only creates schemas, types, and test scaffolding. No new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries.

## Self-Check: PASSED

- `lib/schemas/contact.ts` — FOUND
- `lib/actions/contact.types.ts` — FOUND
- `tests/unit/contact-schema.test.ts` — FOUND
- `tests/e2e/fixtures/contact-helpers.ts` — FOUND
- `components/ui/sheet.tsx` — FOUND
- `components/ui/dropdown-menu.tsx` — FOUND
- `components/ui/select.tsx` — FOUND
- Commits e2ffde5, 67171b8, 4c4a685 — all present in git log
