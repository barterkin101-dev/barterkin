---
phase: 07-pre-launch-seeding
plan: 04
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
  - 07-03
files_modified:
  - scripts/seed-founding-members.mjs
  - docs/outreach-template.md
autonomous: false
requirements:
  - SEED-01
  - SEED-02
  - SEED-05
user_setup:
  - service: Google Form (external, Ashley creates)
    why: "SEED-02 consent-form artifact; response spreadsheet provides data for members[] array"
    dashboard_config:
      - task: "Create the Google Form per the spec in docs/outreach-template.md Section 4; link responses to a Google Sheet titled 'Barterkin Founding Member Responses'; confirm form accepts submissions"
        location: "https://forms.google.com"
  - service: TikTok (external, Ashley DMs personally)
    why: "SEED-01 outreach channel per D-01/D-03"
    dashboard_config:
      - task: "Send 11 personalized DMs per docs/outreach-template.md Section 3 — one per legacy listing. Use base template + per-listing hook. Include Google Form URL + hello@barterkin.com fallback."
        location: "https://www.tiktok.com (Ashley's personal account)"
  - service: hello@barterkin.com inbox (BLOCKING precondition — Plan 01 Section 5 checklist)
    why: "D-02 reply-to; must receive mail before DM #1. Assumption A1."
    dashboard_config:
      - task: "Verify hello@barterkin.com receives mail AND Resend lists it as a verified sender"
        location: "Domain registrar mail routing + Resend dashboard"

must_haves:
  truths:
    - "All 11 legacy listings have been DM'd with a personalized message linking to the Google Form (tracking table in docs/outreach-template.md Section 6 updated)"
    - "Consent responses from the Google Sheet have been manually transcribed into the members[] array in scripts/seed-founding-members.mjs (only consent=Y rows)"
    - "Running `node --env-file=.env.local scripts/seed-founding-members.mjs` seeds consenting founders and prints the Coverage report"
    - "After seed run, SQL query against production returns: ≥30 founding members published, ≥2 distinct counties, ≥3 distinct categories (SEED-05 gate)"
    - "If <30 after first run, Plan 04 loops: Kerry amplifies via TikTok (D-11), new consent responses arrive, members[] array grows, script re-runs idempotently until gate passes"
  artifacts:
    - path: "scripts/seed-founding-members.mjs"
      provides: "The members[] array is populated with real consent-form data (no longer the empty example)"
      contains: "≥ N actual member objects, where N is the count of consent=Y Google Form responses at seed time"
    - path: "docs/outreach-template.md"
      provides: "Updated tracking table (Section 6) — all 11 rows show DM sent date + response Y/N/pending"
      contains: "Filled status cells for every outreach attempt"
  key_links:
    - from: "scripts/seed-founding-members.mjs members[] array"
      to: "Google Sheet responses"
      via: "manual copy-paste by Ashley"
      pattern: "email:|display_name:"
    - from: "Coverage report printout"
      to: "SEED-05 gate satisfied"
      via: "seed script's Section I coverage query"
      pattern: "Distinct counties|Distinct categories|Total founding members"
---

<objective>
Execute the outreach-and-seed loop. This plan is inherently checkpoint-heavy because the critical-path work (sending TikTok DMs, receiving consent responses, manually copying fields into the members[] array, running the seed script) is performed by Ashley personally. The plan's role is to (a) sequence these human actions in a known order, (b) provide the automated scripts Ashley runs, (c) gate progression on the SEED-05 coverage query, (d) close the loop when ≥30/≥2/≥3 gate passes.

Four tasks:

1. **Checkpoint: Preconditions verified** — hello@barterkin.com inbox confirmed, Google Form live, base template approved, GOOGLE_FORM_URL placeholder substituted in docs/outreach-template.md.
2. **Checkpoint: Outreach sent** — Ashley confirms all 11 DMs have been sent; tracking table filled.
3. **Automated: Seed script run + coverage report** — Ashley fills members[] from consent responses, executes the script, captures stdout showing Summary + Coverage.
4. **Checkpoint: SEED-05 gate satisfied** — SQL re-verification of count/counties/categories; if below threshold, loop back (Kerry amplification path D-11).

Purpose: Without this plan, Phases 2/3/5 shipped infrastructure but no data. Phase 7's value is entirely in THIS plan's execution: the directory goes from "empty-feeling" to "≥30 founding members spanning ≥2 counties × ≥3 categories" before public launch.

Output: Production directory contains ≥30 seeded, published, non-banned, email-verified founding-member profiles, verifiable via SQL, with the `Founding member` badge rendering on each.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-pre-launch-seeding/07-CONTEXT.md
@.planning/phases/07-pre-launch-seeding/07-RESEARCH.md
@.planning/phases/07-pre-launch-seeding/07-01-SUMMARY.md
@.planning/phases/07-pre-launch-seeding/07-02-SUMMARY.md
@.planning/phases/07-pre-launch-seeding/07-03-SUMMARY.md
@docs/outreach-template.md
@scripts/seed-founding-members.mjs
@lib/data/georgia-counties.json
@lib/data/categories.ts

<interfaces>
<!-- Category IDs (from lib/data/categories.ts — Phase 3 — confirm current mapping on-read) -->

Legacy category → Barterkin category_id mapping (approximate, from 07-RESEARCH.md Assumption A4):
- food (GG's Goodies, Kelly's Kitchen) → Food & Kitchen (likely id=2; verify in lib/data/categories.ts)
- services (FEYOUG Plumbing) → Trades & Repair (likely id=8)
- crafts (SiR Arts) → Arts & Crafts (likely id=3)
- agriculture (Kerry, Local Homesteader, Tamathaca, Yellow Butterfly Farm) → Outdoors & Animals (likely id=9)
- beauty (Ashley's Nail Studio) → Wellness & Bodywork (likely id=6)
- wellness (Lovelylocs, Donna Marie) → Wellness & Bodywork (id=6)

Ashley MUST read `lib/data/categories.ts` at execution time and use the actual IDs.

Georgia county FIPS mapping (from lib/data/georgia-counties.json — Phase 3 seed data):
- Paulding County (Dallas) — FIPS 13223 (likely; verify)
- Carroll County (Carrollton) — FIPS 13045
- Fulton County (South Fulton) — FIPS 13121
- Douglas County (Douglasville) — FIPS 13097

Ashley MUST read `lib/data/georgia-counties.json` at execution time for exact FIPS values. `counties.id === fips` per Phase 3 D-01.

Seed script invocation (Plan 02):
```bash
node --env-file=.env.local scripts/seed-founding-members.mjs
```

Expected coverage output (from Plan 02 Section I):
```
=== Summary ===
{ "seeded": N, "skipped": M, "emailFailed": K, "errors": [] }

=== Coverage ===
Total founding members published: T
Distinct counties: C (gate: ≥2)
Distinct categories: G (gate: ≥3)
```

SEED-05 gate: T ≥ 30 AND C ≥ 2 AND G ≥ 3.

SQL re-verification query (for Task 4 — independent of script):
```sql
SELECT
  COUNT(*) AS total,
  COUNT(DISTINCT county_id) AS distinct_counties,
  COUNT(DISTINCT category_id) AS distinct_categories
FROM profiles
WHERE founding_member = true
  AND is_published = true
  AND banned = false;
```

Kerry amplification path (D-11):
1. Seed Kerry's profile first (single-row members[] array, run script, verify Kerry appears at /m/kerrys-country-life-slug).
2. DM Kerry: "Your profile is live — would you post to your 1,400+ followers about it? Here's a shareable link." Ashley writes the specific ask.
3. Monitor Google Form for new consent responses as Kerry's followers apply.
4. Each new batch of responses: append to members[], re-run script (idempotent — existing emails skip).
5. Check coverage; repeat until gate passes.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm preconditions before outreach (SEED-01 gate 1)</name>
  <files>docs/outreach-template.md</files>
  <read_first>
    - /Users/ashleyakbar/barterkin/docs/outreach-template.md (Section 5 verification checklist)
    - /Users/ashleyakbar/barterkin/.planning/phases/07-pre-launch-seeding/07-01-SUMMARY.md
  </read_first>
  <action>
    This is a checkpoint:human-verify task. Ashley executes the six verification steps below against production infrastructure and responds with "approved" (proceed) or a specific issue (block). Step 4 requires an actual file edit (substitute the `{GOOGLE_FORM_URL}` placeholder).

    Preconditions to verify before DMing anyone:

    1. **hello@barterkin.com inbox** — Send a test email to hello@barterkin.com from any other address. Confirm it arrives in Ashley's monitored inbox within 5 minutes (Google Workspace alias, forwarding rule, or actual mailbox). Assumption A1 from 07-RESEARCH.md is now verified — no black hole for member email replies.

    2. **Resend verified senders** — Log into Resend dashboard → Domains. Confirm `barterkin.com` is verified AND that `hello@barterkin.com` is listed as a valid sender (either explicitly or as an implicit alias of the verified domain). If only `noreply@` is verified, add `hello@` now OR adjust the seed script's `from` to `noreply@barterkin.com` with a friendly display name like `"Barterkin <noreply@barterkin.com>"`.

    3. **Google Form created and live** — Open https://forms.google.com. Confirm a form titled "Barterkin — Founding Member Consent" exists with all 8 fields from `docs/outreach-template.md` Section 4. Submit one test response (Ashley fills in herself) and verify the response appears in the linked Google Sheet. Capture the form's shareable URL.

    4. **Substitute {GOOGLE_FORM_URL} placeholder** — Open `docs/outreach-template.md`. Find the `{GOOGLE_FORM_URL}` placeholder(s). Replace with the actual form URL from step 3. Save and commit this edit. (The automated verify check below confirms the placeholder is gone.)

    5. **TikTok handles still active (Assumption A3)** — Open TikTok; confirm each of the 11 handles (@ggs_goodies, @kerryscountrylife, @llaaddyybugg, @tamathacal, @feyoug, @ashley, @lovelylocs, @kellymystic119, @yellow_butterfly_farm, @sir_arts, @donnamarie) resolves to an active, non-deleted account. If any handle is dead, mark in tracking table as "handle inactive — could not reach" and proceed with the rest.

    6. **Deployed badge visible** — Open https://barterkin.com/directory on a signed-in session. Directory loads without error. (There may be zero founders seeded yet — that's fine. The SEED-05 empty state is acceptable at this checkpoint; we're verifying the deployment is live, not the data.)

    Reply "approved" to proceed to Task 2 (outreach). Reply with specific issue to block.
  </action>
  <verify>
    <automated>grep -c '{GOOGLE_FORM_URL}' docs/outreach-template.md</automated>
  </verify>
  <done>
    User has replied "approved". The `{GOOGLE_FORM_URL}` placeholder is fully substituted (grep returns 0). hello@barterkin.com inbox, Resend sender, Google Form, TikTok handles, and production directory page are all confirmed live.
  </done>
  <acceptance_criteria>
    - `grep -c '{GOOGLE_FORM_URL}' docs/outreach-template.md` returns 0 (placeholder fully replaced).
    - `grep -c 'forms\.gle\|forms\.google\.com' docs/outreach-template.md` returns ≥ 1 (actual form URL present).
    - User reply is literally "approved" OR names a specific blocking issue (do not proceed on ambiguous responses).
    - Resend domain verification confirmed (screenshot or verbal confirmation; not CI-verifiable).
    - Test email to hello@barterkin.com received (verbal confirmation; not CI-verifiable).
  </acceptance_criteria>
  <resume-signal>Reply "approved" (or name blocking issues)</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute 11 TikTok DMs + update tracking table (SEED-01)</name>
  <files>docs/outreach-template.md</files>
  <read_first>
    - /Users/ashleyakbar/barterkin/docs/outreach-template.md Sections 2, 3, 6 (base template, per-listing personalization, tracking table)
    - /Users/ashleyakbar/barterkin/.planning/phases/07-pre-launch-seeding/07-CONTEXT.md D-01, D-03, D-11 (tone requirement + Kerry amplifier path)
  </read_first>
  <action>
    This is a checkpoint:human-verify task. Ashley sends 11 personalized TikTok DMs (Kerry FIRST), updates the tracking table in Section 6, commits, and reports back.

    For each of the 11 legacy listings:

    1. Open Ashley's TikTok DM UI to the target account.
    2. Compose a DM using `docs/outreach-template.md` Section 2 base template + Section 3 per-listing "Personal hook" merged in.
    3. Substitute the actual Google Form URL (already done in Task 1; copy from Section 4).
    4. Send.
    5. Update Section 6 tracking table: set "DM sent date" to today's date.

    Kerry's Country Life (@kerryscountrylife) receives priority — send this DM FIRST. Once Kerry responds and consents, Kerry's follow-up ask is: "Your profile is live at barterkin.com/m/{Kerry's slug} — would you post about Barterkin to your followers?"

    Recommended daily cadence: all 11 DMs can be sent same day. No platform rate limit for 11 personal DMs on TikTok.

    After all 11 are sent, commit the updated `docs/outreach-template.md` with filled tracking table. Reply with summary: "11/11 DMs sent, Kerry first. {N} responses so far (if any)."

    If any DM fails to send (account locked, blocked, etc.), mark the row in tracking table with reason and proceed.
  </action>
  <verify>
    <automated>grep -c 'DM sent' docs/outreach-template.md</automated>
  </verify>
  <done>
    Tracking table in Section 6 has "DM sent date" populated for ≥10 of 11 listings. Commit message references outreach send. User reply confirms ≥10 DMs sent with Kerry first.
  </done>
  <acceptance_criteria>
    - `docs/outreach-template.md` Section 6 tracking table has DM sent date populated for ≥ 10 of 11 rows (allow 1 missed row for TikTok account issues).
    - Commit message references outreach send (e.g. "docs(07-04): outreach DMs sent").
    - User reply confirms at least 10 DMs were sent AND explicitly mentions Kerry's was sent first.
    - At least 1 consent response is anticipated within 48 hours (process is async; not blocking immediately).
  </acceptance_criteria>
  <resume-signal>Reply with "10/11 DMs sent, Kerry first" (or actual count + any blockers)</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Transcribe consent responses into members[] array + run seed script (SEED-03, SEED-05)</name>
  <files>scripts/seed-founding-members.mjs</files>
  <read_first>
    - /Users/ashleyakbar/barterkin/scripts/seed-founding-members.mjs (the entire file — confirm helpers + main loop structure from Plan 02)
    - /Users/ashleyakbar/barterkin/lib/data/categories.ts (look up actual category IDs — do NOT assume from memory)
    - /Users/ashleyakbar/barterkin/lib/data/georgia-counties.json (look up actual county FIPS — do NOT assume)
    - Google Sheet: "Barterkin Founding Member Responses" (Ashley opens manually and reads)
    - /Users/ashleyakbar/barterkin/.planning/phases/07-pre-launch-seeding/07-RESEARCH.md §"Pitfall 2" (service-role bypasses completeness gate — validate members before run)
  </read_first>
  <action>
    Four sub-steps — edit a data array, then run a script.

    ## Step 3a — Read consent responses from Google Sheet
    Ashley opens the Google Sheet linked to the consent form. Filter to rows where "Consent to migrate listing" = "Yes". For each row, note:
    - Email (preferred email for Supabase account)
    - Display name
    - Bio / description
    - Skills offered (1-5 entries)
    - Skills wanted (0-5 entries)
    - County (member's selection from the 159-county dropdown)
    - Preferred email
    - TikTok handle

    ## Step 3b — Map county name → county_id (FIPS)
    For each member's county, look up the FIPS code in `lib/data/georgia-counties.json`:
    ```bash
    grep -i '"name": "Paulding' lib/data/georgia-counties.json
    # returns: {"fips": 13223, "name": "Paulding"}
    ```
    The `fips` value IS the `county_id` per Phase 3 D-01 (counties.id === fips).

    ## Step 3c — Map legacy category → category_id
    Ashley reads `lib/data/categories.ts` to find the actual ID for each category. Example:
    ```typescript
    // lib/data/categories.ts exports an array:
    // export const CATEGORIES = [
    //   { id: 1, slug: 'food-kitchen', name: 'Food & Kitchen' }, ...
    // ]
    ```
    Use the `id` column. If a member's response doesn't map cleanly to an existing category, use Ashley's best judgment and flag in the SUMMARY.

    ## Step 3d — Edit scripts/seed-founding-members.mjs members[] array
    Open `scripts/seed-founding-members.mjs`. Locate the `const members = [` array (Section D from Plan 02). Replace the commented example with the actual consented members:

    ```javascript
    const members = [
      {
        email: 'kerry-real-email@example.com',
        display_name: "Kerry's Country Life",
        bio: 'Herbal syrups, tinctures, and country living knowledge.',
        county_id: 13223,
        category_id: 9,
        avatar_url: null,
        skills_offered: ['Herbal syrups', 'Tinctures', 'Homesteading advice'],
        skills_wanted: [],
        tiktok_handle: '@kerryscountrylife',
        availability: null,
        accepting_contact: true,
      },
      // ... more members from Google Sheet
    ]
    ```

    Validation checklist per row (before committing):
    - email is a real email (not example.com)
    - display_name is non-empty
    - county_id is an integer in [13001, 13321]
    - category_id is an integer in [1, 10] (exact range depends on lib/data/categories.ts)
    - skills_offered has ≥ 1 entry (required — seed script validateMember() rejects empty)
    - tiktok_handle starts with `@` if present

    PII caveat: If the repo is PUBLIC, Ashley MUST either (a) confirm public exposure is acceptable (founders have implicitly opted in by filling the consent form), (b) redact emails from the commit and keep a private records file, OR (c) confirm repo visibility is private. Flag this decision explicitly in the SUMMARY.

    Commit this edit with message: `feat(07-04): populate founding-members array from consent responses`.

    ## Step 3e — Run the seed script
    ```bash
    cd /Users/ashleyakbar/barterkin
    node --env-file=.env.local scripts/seed-founding-members.mjs
    ```

    Capture the stdout. Expected output shape:
    ```
    ✓ seeded kerry@...example.com (kerrys-country-life) + welcome sent
    ✓ seeded member2@... (display-name-slug) + welcome sent
    ...

    === Summary ===
    { "seeded": N, "skipped": 0, "emailFailed": 0, "errors": [] }

    === Coverage ===
    Total founding members published: N
    Distinct counties: C (gate: ≥2)
    Distinct categories: G (gate: ≥3)
    ```

    If `emailFailed > 0`, log the failed emails — Ashley can re-send welcomes manually from `hello@barterkin.com` inbox.

    If `errors.length > 0`, inspect each error:
    - "validation: ..." — a member row has missing required fields; fix in members[] and re-run.
    - "createUser: ..." — Supabase Auth error (rate limit, invalid email format, etc.); fix and re-run.
    - "insert profile: ..." — DB error (duplicate slug, FK violation); investigate and re-run.

    Re-runs are safe: script is idempotent per SEED-03 Test 2.

    Save the full stdout to `.planning/phases/07-pre-launch-seeding/07-04-seed-run-{timestamp}.log` for the SUMMARY.
  </action>
  <verify>
    <automated>test -f scripts/seed-founding-members.mjs && ls .planning/phases/07-pre-launch-seeding/07-04-seed-run-*.log 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    members[] array populated from consent responses; seed script run; log captured at `.planning/phases/07-pre-launch-seeding/07-04-seed-run-*.log`; errors resolved through re-runs until final log shows `"errors": []`.
  </done>
  <acceptance_criteria>
    - `scripts/seed-founding-members.mjs` members[] array is no longer empty: `grep -c 'email:' scripts/seed-founding-members.mjs` returns ≥ 1.
    - Commented example is replaced with real rows: `grep -c 'kerry-real-email@example.com' scripts/seed-founding-members.mjs` returns 0 (that placeholder is a template, not a real value).
    - A seed-run log file exists: `ls .planning/phases/07-pre-launch-seeding/07-04-seed-run-*.log | wc -l` returns ≥ 1.
    - The log file contains `=== Summary ===` and `=== Coverage ===`: both grep checks return ≥ 1 against the log file.
    - The final log shows `"errors": []` (no errors in summary JSON).
    - At least one seeded line: `grep -c '✓ seeded' $LOG_FILE` returns ≥ 1.
    - Coverage report line present: `grep -c 'Total founding members published' $LOG_FILE` returns ≥ 1.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify SEED-05 coverage gate via SQL + loop until satisfied</name>
  <files>.planning/phases/07-pre-launch-seeding/07-04-seed-run-*.log</files>
  <read_first>
    - `.planning/phases/07-pre-launch-seeding/07-04-seed-run-*.log` (the stdout from Task 3)
    - /Users/ashleyakbar/barterkin/.planning/phases/07-pre-launch-seeding/07-CONTEXT.md D-11 (Kerry amplification path)
    - /Users/ashleyakbar/barterkin/.planning/phases/07-pre-launch-seeding/07-RESEARCH.md §"Common Pitfalls" Pitfall 5 (Resend quota)
  </read_first>
  <action>
    This is a checkpoint:human-verify task. Ashley runs the SEED-05 SQL query against production, checks the gate, and either proceeds to Plan 05 (gate passed) or executes the amplification loop (gate not yet passed).

    1. **Run the SEED-05 SQL query** against production Supabase:
    ```sql
    SELECT
      COUNT(*) AS total,
      COUNT(DISTINCT county_id) AS distinct_counties,
      COUNT(DISTINCT category_id) AS distinct_categories
    FROM profiles
    WHERE founding_member = true
      AND is_published = true
      AND banned = false;
    ```

    Ashley runs this in Supabase Studio SQL Editor (or via `psql`). Capture the result row.

    2. **Check the gate:**
    - If `total ≥ 30 AND distinct_counties ≥ 2 AND distinct_categories ≥ 3` → **GATE PASSED.** Reply "approved - gate passed" and proceed to Plan 05.
    - If below any threshold → **GATE NOT YET PASSED.** Execute amplification loop:

    3. **Amplification loop (if gate not yet passed):**
       a. **Kerry's TikTok post (D-11):** If Kerry's profile is seeded, DM her: "Your founding-member profile is live at barterkin.com/m/{kerry-slug}. Would you post to your 1,400+ followers about Barterkin? Here's a message you could adapt: [short shareable message]."
       b. **Monitor Google Form** over the next 2-7 days for new consent responses.
       c. **For each new response with consent=Y**, append a new object to `members[]` in `scripts/seed-founding-members.mjs` (keeping existing rows untouched — they'll skip on re-run).
       d. **Re-run the script:** `node --env-file=.env.local scripts/seed-founding-members.mjs`. Existing members skip (idempotent per SEED-03 Test 2); new members seed.
       e. **Re-run the SQL query** and re-check gate.
       f. **If still below after Kerry's amplification:** Consider secondary channels:
          - Personal invite to Ashley's network (friends/family in Georgia)
          - Ashley posts from her own TikTok
          - DM another anchor creator (Carroll County, Cobb County focus per D-12)
       g. Repeat (c)-(e) until gate passes OR Ashley decides to soft-launch below 30 with documented rationale (rare exception — requires explicit user approval).

    4. **Resend quota watch (Pitfall 5):** If a single re-run queues >50 welcome emails, split into batches of 50 per day. The seed script logs `emailFailed` — if non-zero, Ashley manually follows up from hello@ inbox.

    5. **Final verification after gate passes:**
       - Visit https://barterkin.com/directory on a signed-in session. Browse and confirm founders appear with the badge.
       - Click into 2-3 founder profiles at `/m/{slug}`. Confirm the detail page also shows the badge inline.
       - Run the SEED-05 SQL one final time and capture the result row.

    Reply with the final SQL result in this format: "GATE PASSED: total={N}, counties={C}, categories={G}" OR "GATE NOT YET PASSED: {N}/{C}/{G} — continuing amplification."
  </action>
  <verify>
    <automated>ls .planning/phases/07-pre-launch-seeding/07-04-seed-run-*.log 2>/dev/null | wc -l</automated>
  </verify>
  <done>
    User reply contains "GATE PASSED: total={N}, counties={C}, categories={G}" where total ≥ 30, counties ≥ 2, categories ≥ 3. Final SQL result captured in SUMMARY. Production badge rendering verified on at least 2-3 /m/{slug} pages and on /directory.
  </done>
  <acceptance_criteria>
    - User reply contains the SQL result row (total, distinct_counties, distinct_categories).
    - Result shows total ≥ 30, distinct_counties ≥ 2, distinct_categories ≥ 3 (the gate).
    - If a soft-launch exception is granted, the SUMMARY documents (a) the actual numbers, (b) the rationale, (c) the commitment to continue seeding post-launch.
    - The tracking table in `docs/outreach-template.md` Section 6 shows final status for all 11 original listings (including decliners).
    - Resend daily cap was NOT exceeded: check Resend dashboard shows <100 sends on any day during this plan.
  </acceptance_criteria>
  <resume-signal>Reply with SQL result: "GATE PASSED: total=N, counties=C, categories=G" (or "not yet - continuing")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ashley's laptop (loaded with prod service-role key) → Production Supabase | HIGH-RISK boundary; any mistake writes to prod. |
| Google Sheet → members[] array | Manual copy is the integrity gate; typos propagate to seeded auth users. |
| Resend free tier (100/day) → script's welcome email loop | Rate limit is real; over-run leaves members seeded-without-welcome. |
| TikTok DM channel → prospective founders | Out-of-band; Ashley's personal reputation is the trust signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | S (Spoofing) | Someone impersonates Ashley in TikTok DMs to a legacy listing | accept | Out of scope; Ashley's personal TikTok account security is her responsibility. DMs are 1:1 with people who already know of the legacy site; trust signal is contextual. |
| T-07-04-02 | T (Tampering) | Typo in members[] array seeds a profile with wrong email → wrong founder seeded | mitigate | (a) Plan 02's `validateMember()` catches missing fields; (b) Ashley visually verifies the diff before committing; (c) idempotent re-run won't duplicate; (d) if discovered, delete via `admin.auth.admin.deleteUser` (cascades) and re-seed with correct email. |
| T-07-04-03 | R (Repudiation) | Member claims they never consented | mitigate | Google Form has timestamped response; Google Sheet is the audit log. Commit history of members[] array is an additional record. 14-day founder-commitment window (D-14) handles first-contact disputes. |
| T-07-04-04 | I (Info Disclosure) | Member emails committed in members[] array → public GitHub | mitigate | (a) Repo is private pre-launch OR (b) Ashley verifies repo privacy before committing the filled members[] AND/OR (c) redact emails from the commit and keep a private records file. The members[] WILL contain PII by design (D-06). Task 3 action block flags this decision explicitly for Ashley to resolve before committing. |
| T-07-04-05 | I (Info Disclosure) | Coverage SQL result posted in STATE.md or SUMMARY with member-identifying info | mitigate | SQL returns aggregated counts only (total, distinct counts) — no row-level PII. Safe to log in SUMMARY. Never log the full SELECT * against profiles in a committed file. |
| T-07-04-06 | D (Denial of Service) | Resend quota burned mid-re-run; remaining welcomes fail | mitigate | Seed script catches + logs email failures; profile-seed succeeds regardless. Ashley batches re-runs at <50/day if running close to cap. Pitfall 5 documented in Plan 02 + re-emphasized here. |
| T-07-04-07 | D (Denial of Service) | Supabase Auth rate limits the `createUser` loop when seeding 30+ at once | accept | Service-role admin.createUser has no documented rate limit for free tier; 30 sequential calls across several minutes is below any reasonable ceiling. If hit, script errors out gracefully per-member and Ashley re-runs (idempotent). |
| T-07-04-08 | E (Elevation) | Ashley runs seed against wrong (staging vs prod) Supabase URL | mitigate | (a) `.env.local` has a single URL; no accidental switch. (b) Cutoff date guard limits runs to pre-launch window. (c) NICE-TO-HAVE: Ashley can add a one-line `console.log('Target:', new URL(URL).host)` at script start if desired. |
| T-07-04-09 | E (Elevation) | Gate-passed decision but actual count doesn't satisfy ≥30 (self-reporting fraud) | mitigate | Task 4 requires user to paste SQL output literally; checkpoint acceptance criteria require the numeric result. Plan 05's pre-launch checklist (C-24/C-25/C-26) re-runs the same query and blocks launch if actually <30. |

**High-severity threats:** T-07-04-04 (PII in public repo) — mitigated by explicit Ashley-confirmation step in Task 3 before committing members[].
</threat_model>

<verification>
After all 4 tasks complete:

1. `docs/outreach-template.md` tracking table (Section 6) filled for all 11 listings.
2. `scripts/seed-founding-members.mjs` members[] array has ≥ 30 entries (indirect via seed log total).
3. `.planning/phases/07-pre-launch-seeding/07-04-seed-run-*.log` shows final Coverage: total ≥ 30, counties ≥ 2, categories ≥ 3, errors=[].
4. Production SQL re-verification: `total ≥ 30 AND distinct_counties ≥ 2 AND distinct_categories ≥ 3`.
5. Spot-check: 2-3 random founders visible at `/directory` with badge rendering; their `/m/{slug}` pages show badge inline.
6. Resend dashboard: no day exceeded 100 sends during plan execution.
</verification>

<success_criteria>
- SEED-01 delivered: all 11 listings contacted per tracking table; Kerry's DM sent first and amplification executed.
- SEED-02 delivered: Google Form live, responses flowing into linked Sheet.
- SEED-05 delivered: production DB has ≥30 founding members published, ≥2 counties, ≥3 categories — verified via independent SQL.
- No PII leak: if repo is public, members[] commit is redacted OR repo privacy is confirmed before commit.
- Seed log captured in `.planning/phases/07-pre-launch-seeding/` for audit trail.
- Production directory page (live at barterkin.com/directory) renders the badge on the seeded founders' cards.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pre-launch-seeding/07-04-SUMMARY.md` documenting:
- Outreach outcome: N/11 DMs sent; M/11 consented; K declined; L non-responsive; filled tracking table.
- Seed script run log (with emails redacted if appropriate).
- Final SEED-05 SQL result: total={N}, counties={C}, categories={G}.
- Any errors encountered + how resolved (re-runs, manual welcome emails, etc.).
- Amplification loop summary: did Kerry post? how many members came from her follower base?
- Resend quota status.
- Handoff to Plan 05: coverage gate passed, directory is populated; next = pre-launch checklist + founder commitment.
</output>
