---
phase: 07-pre-launch-seeding
plan: 05
type: execute
wave: 4
depends_on:
  - 07-04
files_modified:
  - .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md
  - .planning/STATE.md
autonomous: false
requirements:
  - SEED-06

must_haves:
  truths:
    - "PITFALLS §13 'Looks Done But Isn't' checklist has been walked through against PRODUCTION URLs/DB/email infrastructure — every item either (a) verified pass with evidence link or (b) flagged fail with fix-before-launch note"
    - ".planning/STATE.md contains a 'Founder Commitment (SEED-06)' section with Ashley's 14-day first-response pledge and explicit launch/close dates"
    - "All Phase 7 requirement IDs (SEED-01 through SEED-06) are satisfied; Phase 7 is ready for /gsd-verify-work"
  artifacts:
    - path: ".planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md"
      provides: "Final pre-launch verification runbook + audit trail (23 PITFALLS items + 9 Phase 7-specific items)"
      contains: "32 checkbox items each with automated-or-manual verification method + evidence link + PASS/FAIL disposition"
      min_lines: 200
    - path: ".planning/STATE.md"
      provides: "SEED-06 founder commitment (D-14) — publicly committed, git-traced, dated"
      contains: "'## Founder Commitment (SEED-06)' section with Ashley, 14-day window, launch date, close date, tracking method"
  key_links:
    - from: ".planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md"
      to: "PITFALLS.md §13 + Phase 7 RESEARCH.md new items"
      via: "one checkbox row per referenced item"
      pattern: "- \\[(x| )\\]"
    - from: ".planning/STATE.md"
      to: "Phase 7 SEED-06 requirement"
      via: "'Founder Commitment' heading with Ashley's name + 14-day window"
      pattern: "Founder Commitment|14.day"
---

<objective>
Close Phase 7 with two final deliverables:

1. **Pre-launch checklist** — create `.planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` with all 23 items from PITFALLS.md §13 "Looks Done But Isn't" + 9 Phase-7-specific items from 07-RESEARCH.md §"Common Pitfalls (cross-reference to PITFALLS §13)". For each item: (a) how to verify against production, (b) evidence link (screenshot path, SQL result, curl output, etc.), (c) PASS/FAIL disposition. Failing items BLOCK the public launch announcement.

2. **Founder commitment (SEED-06)** — append a `## Founder Commitment (SEED-06)` section to `.planning/STATE.md` with Ashley's 14-day first-response pledge per D-14. Date fields remain placeholders (`YYYY-MM-DD`) until actual launch day — Phase 7 concludes the pre-launch preparation; the launch-day update is a 5-minute human edit on the day itself.

Purpose: PITFALLS §13 is the gate between "we shipped code" and "the code actually works in production." Many items were verified during Phases 2-5 against dev/staging; Phase 7 re-verifies against prod (new URLs, new DNS, post-seed data). The founder commitment is a public pledge that distinguishes pre-launch prep from launch day itself.

Output: Two files. Ashley has a documented pre-launch gate to walk through before public announcement. STATE.md records the SEED-06 operational commitment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/research/PITFALLS.md
@.planning/phases/07-pre-launch-seeding/07-CONTEXT.md
@.planning/phases/07-pre-launch-seeding/07-RESEARCH.md
@.planning/phases/07-pre-launch-seeding/07-04-SUMMARY.md

<interfaces>
<!-- PITFALLS.md §13 "Looks Done But Isn't" — 23 items (from 07-RESEARCH.md §Common Pitfalls cross-ref table) -->
<!-- Each item is re-verified against PRODUCTION (not dev/staging) at this phase -->

23 items from PITFALLS §13 (Table reference: 07-RESEARCH.md §"Common Pitfalls (cross-reference to PITFALLS §13)"):

1. `getUser()` (not `getSession()`) on every server route — verify: `grep -r "getSession(" app/` returns zero
2. Middleware logs out after 1+ hour idle — verify: Playwright sleep test OR manual session expiry test
3. Every table has SELECT/INSERT/UPDATE/DELETE policies — verify: `pg_policies` query
4. Views have `security_invoker = true` — verify: `\d+ <view>` (N/A for this project — no views)
5. Supabase Advisor shows zero red lints — verify: Supabase Dashboard visual check
6. Anon role cannot SELECT unauthorized rows — verify: SQL test as anon (tests/unit/directory-rls-visibility.test.ts)
7. `grep -r service_role .next/static` empty — verify: post-build grep against prod build output
8. Avatar path traversal rejected — verify: manual upload test against prod storage
9. Bucket `file_size_limit` + `allowed_mime_types` set — verify: Supabase Dashboard storage settings
10. Pages calling `getUser()` have `dynamic = 'force-dynamic'` — verify: grep check against app/
11. SPF/DKIM/DMARC pass — verify: `dig` commands + mail-tester.com ≥9/10 against PROD DNS
12. Apple Communication Email Service registered — SKIP (project does not use Apple Sign-In per PROJECT.md; deferred to Capacitor milestone)
13. Contact Reply-To server-assigned, not body-derived — verify: code inspection of Phase 5 Edge Function
14. Same sender → same recipient blocked after 1 msg/24h — verify: `curl` × 2 against prod contact endpoint
15. Sender hits 5/24h total cap — verify: scripted test against prod
16. Blocked sender cannot send to blocker — verify: E2E test against prod
17. Report creates row + emails admin — verify: E2E against prod
18. "Contact button pressed" PostHog event fires — verify: test click + check PostHog dashboard
19. Directory first paint shows ≥20 profiles OR category/county chooser — verify: visual check on /directory (SEED-05 already satisfies; this is the re-check)
20. ToS + Privacy linked from footer — verify: visual check on /
21. Signup requires county selection — verify: manual flow against prod signup
22. Profile hidden until `email_confirmed_at IS NOT NULL` — verify: SQL filter against prod
23. Manifest valid; icons exist; Lighthouse PWA ≥90 — verify: Lighthouse run against prod URL

9 Phase 7-specific items (from 07-RESEARCH.md §"Items specific to Phase 7 (new)"):

24. SEED-05 gate: ≥30 founding members published (re-verified after Plan 04)
25. ≥2 distinct counties (re-verified)
26. ≥3 distinct categories (re-verified)
27. Founding badge renders on /directory card for founding_member=true profiles (re-verified against prod)
28. Founding badge does NOT render for founding_member=false profiles (re-verified against prod)
29. STATE.md contains D-14 Founder Commitment block with explicit launch date
30. Google Form URL live and tested
31. docs/outreach-template.md reviewed and approved
32. Welcome email deliverability spot-check (send to Ashley's inbox; verify formatting + from-address + reply-target)

D-14 Founder Commitment entry format (from 07-RESEARCH.md §Example 3, verbatim):
```markdown
## Founder Commitment (SEED-06)

**Founder:** Ashley (solo operator)
**Commitment:** I will personally reply to every contact request received in the first 14 days post-launch.
**Launch date:** YYYY-MM-DD  <!-- fill at launch -->
**Commitment window closes:** YYYY-MM-DD  <!-- launch + 14 days -->
**Tracking:** PostHog `contact_initiated` event count vs manual reply log in STATE.md

_First recorded: 2026-04-XX by Phase 7._
```

The `YYYY-MM-DD` placeholders stay literal in this plan; they're substituted with actual dates on launch day (a 5-minute edit outside Phase 7's scope). This is a deliberate design — Phase 7 ends when the commitment is RECORDED, not when the window opens.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the pre-launch checklist file with all 32 items (Claude's Discretion from CONTEXT.md)</name>
  <files>.planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md</files>
  <read_first>
    - /Users/ashleyakbar/barterkin/.planning/research/PITFALLS.md (read §13 in full — 23 items are the source of truth)
    - /Users/ashleyakbar/barterkin/.planning/phases/07-pre-launch-seeding/07-RESEARCH.md §"Common Pitfalls (cross-reference to PITFALLS §13)" (table mapping each item → where already addressed)
    - /Users/ashleyakbar/barterkin/.planning/phases/07-pre-launch-seeding/07-RESEARCH.md §"Items specific to Phase 7 (new)" (9 Phase-7 items)
    - /Users/ashleyakbar/barterkin/.planning/STATE.md (look up project URLs: barterkin.com is the prod URL)
  </read_first>
  <action>
    Create `.planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` (does NOT exist) with the following structure. Each checklist item is a self-contained unit: a unique ID (C-01..C-32), title, verification method, expected evidence, and PASS/FAIL checkbox.

    ## Section 1 — Header + Overview (lines 1-25)
    ```markdown
    ---
    phase: 07-pre-launch-seeding
    plan: 05
    created: {today's date}
    status: pending_execution
    ---

    # Phase 7 — Pre-Launch Verification Checklist

    **Source:** PITFALLS.md §13 "Looks Done But Isn't" (items C-01 to C-23) + 07-RESEARCH.md §"Items specific to Phase 7 (new)" (items C-24 to C-32)

    **Purpose:** Every item below MUST be verified against PRODUCTION (not dev/staging) before the public launch announcement. Items verified during earlier phases are re-verified here because prod DNS/config/data has changed since then.

    **Pass criteria:** Every item checked off OR marked "SKIP with rationale" OR flagged "FAIL — fix before launch". Any unchecked FAIL blocks launch.

    **How to use:** Walk through each item, capture evidence (screenshot path, SQL result, curl output, dashboard URL), mark checkbox, fill the Disposition column. Commit the filled file as proof of the gate.

    ## Legend

    - [ ] pending
    - [x] PASS (verified against prod with evidence)
    - [!] FAIL (needs fix before launch — see Notes)
    - [-] SKIP (N/A per project constraints — see Notes)
    ```

    ## Section 2 — The 23 PITFALLS §13 items (C-01..C-23) (lines 27-170)

    Write each of the 23 items as a self-contained subsection. Template:

    ```markdown
    ### C-01: `getUser()` (not `getSession()`) on every server route

    - [ ] Verified against production
    - **Why it matters:** `getSession()` reads the cookie without revalidating — spoofable. `getUser()` calls the Supabase Auth server to verify the JWT.
    - **Verification method:** `grep -rn "getSession(" app/ lib/` (from repo root against the deployed commit)
    - **Expected evidence:** Zero matches. If matches exist, they must be inside client components ONLY (not server components, middleware, or server actions).
    - **Evidence link:** `<paste grep output here>`
    - **Disposition:** PASS | FAIL | SKIP
    - **Notes:** _(fix-before-launch note if FAIL)_
    ```

    Apply the template to all 23 items. For each, use the table in `07-RESEARCH.md §"Common Pitfalls (cross-reference to PITFALLS §13)"` to fill in the "Verification method" — it lists the exact grep/SQL/Playwright command or the Supabase Studio visual path.

    Item C-04 (views have security_invoker=true) — mark disposition as SKIP with note: "No views in this project at MVP. Re-check if any view is added in future phases."

    Item C-12 (Apple Sign-In) — mark disposition as SKIP with note: "Project does not use Apple Sign-In per PROJECT.md (deferred to Capacitor milestone). Re-verify this item when Capacitor milestone ships."

    Item C-20 (ToS + Privacy linked from footer) — verification note: "Phase 2 shipped routes at `/legal/tos`, `/legal/privacy` (NOT `/terms`, `/privacy` as PITFALLS §13 originally wrote). Reconcile: the actual routes are the canonical path."

    ## Section 3 — The 9 Phase-7-specific items (C-24..C-32) (lines 172-250)

    Follow the same template. Specific items:

    - **C-24:** SEED-05 total ≥ 30 — verification: same SQL query from Plan 04 Task 4. Paste the most recent result here.
    - **C-25:** SEED-05 distinct_counties ≥ 2 — same query.
    - **C-26:** SEED-05 distinct_categories ≥ 3 — same query.
    - **C-27:** Founding badge renders on `/directory` for founding_member=true — manual browse test against prod; attach screenshot path.
    - **C-28:** Founding badge does NOT render on `/directory` for founding_member=false — must test with a non-founder profile visible in directory. Paste URL + visual check result.
    - **C-29:** `.planning/STATE.md` contains the Founder Commitment block — grep verification: `grep -c 'Founder Commitment' .planning/STATE.md`. This is completed by Task 2 of this plan.
    - **C-30:** Google Form URL live and tested — paste the live URL and one self-submitted response row's timestamp.
    - **C-31:** `docs/outreach-template.md` reviewed and approved by Ashley — boolean Ashley confirmation.
    - **C-32:** Welcome email deliverability spot-check — Ashley runs the script against her own email address (staging Supabase if preferred) and confirms: (a) from = `hello@barterkin.com`, (b) reply-to works (she replies, it lands in her own monitored inbox), (c) subject + text render cleanly in Gmail / iOS Mail.

    ## Section 4 — Launch decision (lines 252-280)
    ```markdown
    ## Launch Decision

    **Gate:** All items above must be PASS or SKIP (with documented rationale). Any FAIL blocks launch.

    **Evaluator:** Ashley (solo operator)

    **Outcome:** [ ] APPROVED TO LAUNCH | [ ] BLOCKED — see failing items

    **Approval date:** YYYY-MM-DD (fill when gate passes)

    **Public launch announcement target:** YYYY-MM-DD (fill when decided)

    **Notes:** _(room for any last-minute context)_
    ```

    ## Section 5 — Item cross-reference table (lines 282-320)

    Copy-paste the cross-reference table from `07-RESEARCH.md §"Common Pitfalls (cross-reference to PITFALLS §13)"` as Section 5. This preserves the mapping "item X was first addressed in Phase Y" for audit.

    ## File constraints
    - Standard Markdown
    - YAML frontmatter at top (phase, plan, created, status)
    - Use `{today's date}` in frontmatter — substitute actual ISO date (e.g., 2026-04-21)
    - 32 unique checklist items numbered C-01 through C-32
    - Every item has exactly ONE checkbox (pending state on creation; Ashley flips later)
    - Every item has a "Disposition" line with PASS | FAIL | SKIP placeholders
    - Total length: 200-350 lines
  </action>
  <verify>
    <automated>test -f .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md && grep -c '^### C-' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md && grep -c 'Disposition' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md && grep -c 'Launch Decision' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md && wc -l .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `.planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md`.
    - Contains YAML frontmatter: `head -5 .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md | grep -c '^---' ` returns 2 (opening + closing `---`).
    - Contains exactly 32 item headings (C-01 through C-32): `grep -c '^### C-' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` returns 32.
    - Every item has a Disposition row: `grep -c 'Disposition' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` returns ≥ 32.
    - Contains Launch Decision section: `grep -c 'Launch Decision' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` returns ≥ 1.
    - File length in expected range: `wc -l .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` returns value in [200, 400].
    - Legend explains disposition symbols: `grep -c '\\[x\\] PASS\\|\\[!\\] FAIL\\|\\[-\\] SKIP' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` returns ≥ 3.
    - Item C-12 (Apple Sign-In) is marked SKIP with rationale: `awk '/^### C-12/,/^### C-13/' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md | grep -c 'SKIP\|deferred\|Capacitor'` returns ≥ 1.
    - Item C-29 references Founder Commitment (which Task 2 creates): `awk '/^### C-29/,/^### C-30/' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md | grep -c 'Founder Commitment'` returns ≥ 1.
    - Section 2 (PITFALLS §13 items) contains 23 items: `grep -c '^### C-0[1-9]\\|^### C-1[0-9]\\|^### C-2[0-3]' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` returns 23.
    - Section 3 (Phase-7-new items) contains 9 items: `grep -c '^### C-2[4-9]\\|^### C-3[0-2]' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` returns 9.
  </acceptance_criteria>
  <done>
    Checklist file committed with all 32 items, each in pending state; Ashley has a complete runbook to execute before launch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Append Founder Commitment section to STATE.md (SEED-06, D-14)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - /Users/ashleyakbar/barterkin/.planning/STATE.md (read the current file in full to know where to append — at the end, after the "Evolution" section)
    - /Users/ashleyakbar/barterkin/.planning/phases/07-pre-launch-seeding/07-CONTEXT.md D-14 (exact entry format)
    - /Users/ashleyakbar/barterkin/.planning/phases/07-pre-launch-seeding/07-RESEARCH.md §"Example 3" (verbatim STATE.md entry)
  </read_first>
  <action>
    Append a new section to `.planning/STATE.md`. The file ends with a "Evolution" section (currently around line 185-186) followed by a final italicized footer line. Insert the Founder Commitment block BETWEEN the "Evolution" section and the final footer line.

    Locate the existing footer (last non-blank lines look like):
    ```
    ---
    *State initialized: 2026-04-17 at roadmap creation*
    *Last updated: 2026-04-18 (Plan 01-03 complete)*
    ```

    Insert the new section BEFORE that footer. The exact content to insert:

    ```markdown

    ---

    ## Founder Commitment (SEED-06)

    **Founder:** Ashley (solo operator)
    **Commitment:** I will personally reply to every contact request received in the first 14 days post-launch.
    **Launch date:** YYYY-MM-DD  <!-- fill on launch day (5-min edit) -->
    **Commitment window closes:** YYYY-MM-DD  <!-- launch + 14 days -->
    **Tracking:** PostHog `contact_initiated` event count vs manual reply log in this section.

    ### Reply Log (filled post-launch)

    | Date | Sender (anonymized) | Recipient | Replied within 14-day window? |
    |------|---------------------|-----------|-------------------------------|
    | _(first entry after launch)_ | — | — | — |

    ### Context

    This commitment is the SEED-06 requirement. The empty-directory cold-start problem (PITFALLS §13) is solved not only by seeding ≥30 founders (SEED-05) but by ensuring every first contact receives a real response during the fragile first two weeks. Without this, the first 10 senders who get silence will shape the platform's reputation in exactly the wrong direction.

    _First recorded: 2026-04-21 by Phase 7 Plan 05._
    ```

    Update the existing footer's "Last updated" line to reference Phase 7:
    - Before: `*Last updated: 2026-04-18 (Plan 01-03 complete)*`
    - After: `*Last updated: 2026-04-21 (Phase 7 Plan 05 — Founder Commitment recorded)*`

    Do NOT modify any other section of STATE.md. Do NOT change the frontmatter, the Current Position, the Phase Status Grid, or the Accumulated Context. Those are owned by `/gsd-transition` and auto-update during phase completion.

    File constraints:
    - Use the exact H2 heading `## Founder Commitment (SEED-06)` (required for grep acceptance check)
    - Keep `YYYY-MM-DD` as literal placeholder text (not today's date — actual launch date unknown at this step)
    - "First recorded" date = today's ISO date (substitute 2026-04-21 or current date)
    - Maintain the existing horizontal rule (`---`) separator before the new section AND before the final footer
  </action>
  <verify>
    <automated>grep -c '^## Founder Commitment (SEED-06)' .planning/STATE.md && grep -c '14 days post-launch' .planning/STATE.md && grep -c 'YYYY-MM-DD' .planning/STATE.md && grep -c 'Phase 7 Plan 05' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - STATE.md contains the exact H2 heading: `grep -c '^## Founder Commitment (SEED-06)' .planning/STATE.md` returns 1.
    - Commitment text references "14 days post-launch": `grep -c '14 days post-launch' .planning/STATE.md` returns 1.
    - Launch date placeholder is literal YYYY-MM-DD: `grep -c 'YYYY-MM-DD' .planning/STATE.md` returns ≥ 2 (launch date + window-close date).
    - "Reply Log" sub-section present: `grep -c 'Reply Log' .planning/STATE.md` returns 1.
    - Footer "Last updated" line references Phase 7 Plan 05: `grep -c 'Phase 7 Plan 05' .planning/STATE.md` returns 1.
    - Ashley is named as founder: `grep -c 'Founder:.*Ashley' .planning/STATE.md` returns 1.
    - Tracking reference to PostHog present: `grep -c 'PostHog \\`contact_initiated\\`\\|posthog.*contact_initiated' .planning/STATE.md` returns ≥ 1.
    - File still valid Markdown: the file's existing structure is unchanged for all other sections (verify by `diff` between the old and new file, the only additions should be the new section + the "Last updated" line change).
    - The existing Phase Status Grid, Current Position, Session Continuity sections are UNCHANGED.
  </acceptance_criteria>
  <done>
    STATE.md has the Founder Commitment section; SEED-06 requirement is traceable to a committed, timestamped entry; Ashley edits placeholders to real dates on launch day.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Walk through the pre-launch checklist + record final dispositions</name>
  <files>.planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md</files>
  <read_first>
    - `.planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` (the file from Task 1 — 32 items to verify)
    - /Users/ashleyakbar/barterkin/.planning/research/PITFALLS.md §13 (source of truth for items C-01 to C-23)
  </read_first>
  <action>
    This is a checkpoint:human-verify task. Ashley walks through each of the 32 items C-01 to C-32 in `.planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md`. For each item:

    1. Execute the verification method listed in the item's "Verification method" row.
    2. Paste the output/evidence into the "Evidence link" row (or attach a screenshot in `.planning/phases/07-pre-launch-seeding/evidence/` if visual).
    3. Flip the checkbox from `[ ]` to `[x]`, `[!]`, or `[-]` per Legend.
    4. If `[!] FAIL`, add a fix-before-launch note and address the issue; re-run verification after fix.
    5. Fill the Disposition row: PASS | FAIL | SKIP.

    **Skip rationale is acceptable for:**
    - C-04 (views security_invoker) — no views in project
    - C-12 (Apple Sign-In) — deferred per PROJECT.md

    **Every other item must PASS.** If any FAIL remains by the time Ashley reaches the bottom, the checklist itself blocks launch. Fix the root cause (not the checklist wording) and re-verify.

    **Expected walk-through time:**
    - C-01..C-10 (auth + RLS + middleware re-checks): 15-20 minutes (mostly grep + SQL)
    - C-11 (mail deliverability): 10 minutes (send to mail-tester.com; wait for score)
    - C-13..C-18 (contact relay + trust re-checks): 20-30 minutes (curl + E2E test runs)
    - C-19..C-23 (directory + signup + manifest re-checks): 15 minutes
    - C-24..C-32 (Phase-7-specific): 20 minutes (SQL + badge browse check + welcome email spot-check)

    Total: ~90-120 minutes of focused walk-through.

    Commit the filled checklist with message: `docs(07-05): pre-launch checklist walk-through complete — {N passed}/{M failed}/{K skipped}`.

    Reply with: "CHECKLIST COMPLETE: {P} PASS, {F} FAIL, {S} SKIP" where F=0 for launch approval.

    If F > 0, state which items failed and the fix ETA. Plan 05 remains open until F=0.
  </action>
  <verify>
    <automated>bash -c 'pending=$(grep -c "^- \[ \]" .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md); fail=$(grep -c "^- \[!\]" .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md); echo "pending=$pending fail=$fail"; test "$pending" -eq 0 -a "$fail" -eq 0'</automated>
  </verify>
  <done>
    All 32 checklist items have a final disposition (no pending `[ ]` remain); zero FAIL items (`[!]`); at most 2 SKIP items (C-04, C-12); Launch Decision section marked "APPROVED TO LAUNCH"; user reply confirms "CHECKLIST COMPLETE: P PASS, 0 FAIL, S SKIP".
  </done>
  <acceptance_criteria>
    - No pending checkboxes remain in the file: `grep -c '^- \[ \]' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` returns 0 (all 32 items checked).
    - At least 30 items are PASS: `grep -c '^- \[x\]' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` returns ≥ 30.
    - Zero items are FAIL: `grep -c '^- \[!\]' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` returns 0.
    - Up to 2 items are SKIP (C-04, C-12): `grep -c '^- \[-\]' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` returns ≤ 2.
    - Launch Decision section reflects outcome: one of "APPROVED TO LAUNCH" or "BLOCKED" is checked.
    - User reply contains numeric summary of PASS/FAIL/SKIP.
    - Every item has an Evidence link filled (not empty placeholder).
  </acceptance_criteria>
  <resume-signal>Reply: "CHECKLIST COMPLETE: {P} PASS, {F} FAIL, {S} SKIP" (F must be 0 to proceed to launch)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checklist (`.planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md`) → prod URLs/DB | Verification commands touch prod; output is evidence for the audit trail. |
| STATE.md → public commitment | Once committed, the Founder Commitment is a git-traceable promise. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | R (Repudiation) | Later claim that the checklist was rushed/fudged | mitigate | Each item requires an "Evidence link" — grep / SQL / screenshot path. Acceptance check `grep -c 'Evidence link:.*_$'` rejects empty evidence. Git history shows when each item was checked off. |
| T-07-05-02 | T (Tampering) | Checklist items skipped without rationale | mitigate | Acceptance checks gate SKIP count to ≤ 2 (only C-04 + C-12 legitimately skip). Any additional SKIP must be added with explicit rationale note. |
| T-07-05-03 | I (Info Disclosure) | Evidence files contain production PII | mitigate | Evidence snippets are aggregated counts, grep output (patterns not data), screenshot paths, or SQL row-counts. Do NOT paste full SELECT * rows with member emails. Review each evidence entry before commit. |
| T-07-05-04 | R (Repudiation) | Founder Commitment dates left as YYYY-MM-DD forever | accept | The placeholder is intentional — Phase 7 can't know the launch date. Launch-day edit is a 5-minute task outside Phase 7's scope. The STATE.md "Evolution" section documents this substitution pattern. |
| T-07-05-05 | E (Elevation) | Checklist approval flagged PASS while FAIL items exist | mitigate | Task 3 acceptance criteria `grep -c '^- \\[!\\]'` returns 0 is REQUIRED for Plan 05 completion. Any FAIL blocks downstream launch decision. |
| T-07-05-06 | T (Tampering) | STATE.md Founder Commitment section deleted by accident | mitigate | SEED-06 verification in Phase 7 verification step (PITFALLS C-29) greps for the section. Git history preserves the commit even if the file is later mangled. |

**High-severity threats:** None. This plan produces documentation, not code.
</threat_model>

<verification>
After both tasks complete:

1. `test -f .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` — file exists.
2. `grep -c '^### C-' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` returns 32.
3. `grep -c '^## Founder Commitment (SEED-06)' .planning/STATE.md` returns 1.
4. After Task 3 walk-through: `grep -c '^- \\[ \\]' .planning/phases/07-pre-launch-seeding/07-PRELAUNCH-CHECKLIST.md` returns 0 and `grep -c '^- \\[!\\]' ...` returns 0.
5. `pnpm typecheck` still passes (documentation change shouldn't affect code — sanity check).
6. Phase 7 gate ready for `/gsd-verify-work` execution.
</verification>

<success_criteria>
- SEED-06 delivered: STATE.md contains the Founder Commitment block with Ashley's 14-day pledge and tracking method.
- PITFALLS §13 sweep completed: 32 items walked through against production, evidence captured, dispositions recorded.
- Phase 7 ready to transition: no outstanding requirements, no blocking FAILs.
- All 6 Phase 7 requirement IDs (SEED-01 through SEED-06) satisfied across plans 01-05.
</success_criteria>

<output>
After completion, create `.planning/phases/07-pre-launch-seeding/07-05-SUMMARY.md` documenting:
- Checklist walk-through: P/F/S counts, time spent, any FAIL items and how resolved
- Founder Commitment: recorded; placeholder dates noted for launch-day substitution
- Final Phase 7 requirement coverage table (SEED-01..06 → which plan satisfied each)
- Handoff to `/gsd-verify-work`: what the verifier should check (seed count, badge render, checklist PASS, Founder Commitment present)
- Post-launch follow-ups (not Phase 7 scope):
  - Launch day: substitute YYYY-MM-DD placeholders in STATE.md Founder Commitment
  - Day 1-14: update Reply Log table in STATE.md as contacts arrive
  - Day 15: close the commitment window; transition to steady-state ops
</output>
